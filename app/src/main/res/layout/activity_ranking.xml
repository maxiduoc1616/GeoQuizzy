<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurfaceContainerLowest"
    tools:context=".RankingActivity">

    <!-- Barra de herramientas superior para el título y el botón de volver -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar_ranking"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:title="Ranking de Puntuaciones" />

    <!-- CardView para encapsular la lista de ranking -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_view_ranking_list"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="16dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="4dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar_ranking">

        <!-- RecyclerView para mostrar la lista de ranking -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_ranking"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingVertical="8dp"
            tools:listitem="@layout/ranking_item" />

    </com.google.android.material.card.MaterialCardView>

    <!-- Mensaje para cuando no hay puntuaciones -->
    <TextView
        android:id="@+id/tv_empty_ranking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Aún no hay puntuaciones.\n¡Juega para ser el primero!"
        android:textAppearance="?attr/textAppearanceTitleMedium"
        android:textColor="?android:attr/textColorSecondary"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/card_view_ranking_list"
        app:layout_constraintEnd_toEndOf="@id/card_view_ranking_list"
        app:layout_constraintStart_toStartOf="@id/card_view_ranking_list"
        app:layout_constraintTop_toTopOf="@id/card_view_ranking_list"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>