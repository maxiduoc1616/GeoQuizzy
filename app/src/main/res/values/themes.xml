<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GeoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/geist_font_family</item>
        <item name="fontFamily">@font/geist_font_family</item>
    </style>

    <style name="Theme.GeoApp" parent="Base.Theme.GeoApp" />
</resources>